'use client';

import { type Store } from './StoreSearchSheet';

interface MapConfirmStepProps {
  store?: Store | null;
  onBack: () => void;
}

export const MapConfirmStep = ({
  store: _store,
  onBack: _onBack,
}: MapConfirmStepProps) => {
  return null;
};
